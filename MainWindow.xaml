<Window x:Class="Explore.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Explore"
        mc:Ignorable="d"
        Title="MainWindow" Height="760" Width="1280">

	<!-- Ctrl+I to run AI status check -->
	<Window.InputBindings>
		<KeyBinding Command="{x:Static local:MainWindow.CheckAICommand}"
					Modifiers="Control" Key="I"/>
	</Window.InputBindings>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Menu -->
		<Menu Grid.Row="0">
			<MenuItem Header="_ファイル(F)"/>
			<MenuItem Header="_編集(E)"/>
			<MenuItem Header="_表示(V)">
				<MenuItem Header="AI状態確認" Click="Menu_CheckAI_Click" InputGestureText="Ctrl+I"/>
			</MenuItem>
		</Menu>

		<!-- Top bar (view switches) -->
		<DockPanel Grid.Row="1" LastChildFill="True" Margin="8,6,8,6">
			<StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
				<ToggleButton x:Name="FileToggle"   Content="ファイル"   Click="SwitchToFile"   MinWidth="80"  Padding="10,4" />
				<ToggleButton x:Name="RenameToggle" Content="AIリネーム" Click="SwitchToRename" MinWidth="100" Padding="10,4" Margin="8,0,0,0"/>
				<ToggleButton x:Name="BuildToggle"  Content="構築"       Click="SwitchToBuild"  MinWidth="80"  Padding="10,4" Margin="8,0,0,0"/>
				<ToggleButton x:Name="ChatToggle"   Content="チャット"   Click="SwitchToChat"   MinWidth="80"  Padding="10,4" Margin="8,0,0,0"/>
			</StackPanel>
		</DockPanel>

		<!-- Main content host -->
		<ContentControl x:Name="MainViewBox" Grid.Row="2" />
	</Grid>
</Window>
